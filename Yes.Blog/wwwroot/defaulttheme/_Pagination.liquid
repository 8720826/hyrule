<nav class="pagination">


	{%  for page in (1..Model.Articles.TotalPage) %}
	{% if page == Model.Articles.PageIndex %}
	<a class="enabled  current" href="?page={{ page }}">{{ page }}</a>
	{% else %}

	<a class="enabled" href="?page={{ page }}">{{ page }}</a>
	{% endif %}


	{% endfor %}

</nav>